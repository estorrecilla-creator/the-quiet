services:
  - type: web
    name: the-quiet
    env: docker
    plan: starter
    healthCheckPath: /login
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: APP_PASSWORD
        sync: false
      - key: FLASK_SECRET_KEY
        generateValue: true
